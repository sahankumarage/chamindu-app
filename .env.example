# ---- C Printing environment variables ----
# The app currently runs in no-database demo mode, so NO env vars are required.
# Optionally set a strong secret for signing login sessions in production:
#   openssl rand -base64 32
JWT_SECRET=
